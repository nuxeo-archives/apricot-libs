This is a dummy OSGi integration of jaxws-rt 2.2.0 needed only when developing OpenCmis bridge in PDE.
This library is not needed at runtime (and will not work) it is only used to avoid breaking 
PDE plugins that uses optional imports on com.sun.ws.*

