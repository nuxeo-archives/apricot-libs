<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    	 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">


  <modelVersion>4.0.0</modelVersion>
  <groupId>org.nuxeo.osgi</groupId>
  <artifactId>nuxeo-osgi-tycho</artifactId>
  <version>5.4.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Nuxeo OSGi Tycho</name>
  <description>Provide common OSGi tycho build directives.</description>

  <modules>
    <module>../ide/rebrand</module>
    <module>../ide/project</module>
    <module>../ide/project-generator</module>
    <module>../ide/project-feature</module>
    <module>../ide/project-site</module>
    <module>../plugins</module>
    <module>../plugins/h2/connector</module>
    <module>../plugins/h2/functions</module>
    <module>../products/core-server/config</module>
    <module>../products/core-server/features/equinox</module>
    <module>../products/core-server/features/equinox-sdk</module>
    <module>../products/core-server/features/base</module>
    <module>../products/core-server/features/base-sdk</module>
    <module>../products/core-server/features/h2</module>
    <module>../products/core-server/features/h2-sdk</module>
    <module>../products/core-server/features/web</module>
    <module>../products/core-server/features/web-sdk</module>
    <module>../products/core-server/features/opencmis</module>
    <module>../products/core-server/features/opencmis-sdk</module>
    <module>../products/core-server/features/sdk</module>
    <module>../products/core-server/product</module>
  </modules>

  <scm>
    <connection>scm:hg:http://hg.nuxeo.org/sandbox/nuxeo-osgi</connection>
    <developerConnection>scm:hg:https://hg.nuxeo.org/sandbox/nuxeo-osgi</developerConnection>
    <url>http://hg.nuxeo.org/nuxeo/</url>
  </scm>

  <properties>
    <tycho-version>0.10.0</tycho-version>
    <orbit-version>current</orbit-version>
    <eclipse-version>current</eclipse-version>
    <nuxeo-version>current</nuxeo-version>
    <nuxeo-osgi-url>http://osgi.nuxeo.org/p2</nuxeo-osgi-url>
    <equinox-site>${nuxeo-osgi-url}/mirrors/eclipse/updates/${eclipse-version}</equinox-site>
    <orbit-site>${nuxeo-osgi-url}/mirrors/eclipse/orbit/${orbit-version}</orbit-site>
    <libs-site>${nuxeo-osgi-url}/libs/${nuxeo-version}/repository</libs-site>
  </properties>

  <profiles>
    <profile>
      <id>tycho-offline</id>
      <properties>
	<nuxeo-osgi-url>http://localhost/p2</nuxeo-osgi-url>
      </properties>
    </profile>
  </profiles>

  <repositories>
    <repository>
      <id>equinox-site</id>
      <layout>p2</layout>
      <url>${equinox-site}</url>
    </repository>
    <repository>
      <id>libs-site</id>
      <layout>p2</layout>
      <url>${libs-site}</url>
    </repository>
  </repositories>

  <build>
    <plugins>
      <plugin>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>tycho-maven-plugin</artifactId>
	<version>${tycho-version}</version>
	<extensions>true</extensions>
      </plugin>
      <plugin>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>target-platform-configuration</artifactId>
	<version>${tycho-version}</version>
	<configuration>
	  <resolver>p2</resolver>

          <environments>
            <environment>
              <os>linux</os>
              <ws>gtk</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>linux</os>
              <ws>gtk</ws>
              <arch>x86</arch>
            </environment>
            <environment>
              <os>win32</os>
              <ws>win32</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>win32</os>
              <ws>win32</ws>
              <arch>x86</arch>
            </environment>
<!--
            <environment>
              <os>macosx</os>
              <ws>carbon</ws>
              <arch>x86</arch>
            </environment>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>x86</arch>
            </environment>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>x86_64</arch>
            </environment>
 -->
	  </environments>

	</configuration>
      </plugin>
      <plugin>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>maven-osgi-packaging-plugin</artifactId>
	<version>${tycho-version}</version>
	<configuration>
	  <archiveSite>true</archiveSite>
	</configuration>
      </plugin>
      <plugin>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>maven-osgi-source-plugin</artifactId>
	<version>${tycho-version}</version>
	<executions>
	  <execution>
	    <id>plugin-source</id>
	    <goals>
	      <goal>plugin-source</goal>
	    </goals>
	  </execution>
	</executions>
      </plugin>
      <plugin>
	<inherited>true</inherited>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>tycho-p2-publisher-plugin</artifactId>
	<version>${tycho-version}</version>
	<configuration>
	  <publishArtifacts>true</publishArtifacts>
	</configuration>
      </plugin>
      <plugin>
	<groupId>org.sonatype.tycho</groupId>
	<artifactId>tycho-p2-director-plugin</artifactId>
	<version>${tycho-version}</version>
      </plugin>
    </plugins>
  </build>
  <!--
     <build>
       <directory>${project.basedir}/target</directory>

       <plugins>
	 <plugin>
    	   <groupId>org.codehaus.mojo</groupId>
    	   <artifactId>build-helper-maven-plugin</artifactId>
    	   <version>1.3</version>
	 </plugin>
	 <plugin>
           <groupId>org.sonatype.tycho</groupId>
           <artifactId>target-platform-configuration</artifactId>
    	   <version>${tycho-version}</version>
           <configuration>
             <resolver>p2</resolver>
           </configuration>
	 </plugin>
	 <plugin>
	   <groupId>org.sonatype.tycho</groupId>
	   <artifactId>maven-osgi-packaging-plugin</artifactId>
	   <version>${tycho-version}</version>
	   <configuration>
	     <archiveSite>true</archiveSite>
	   </configuration>
	 </plugin>
	 <plugin>
           <groupId>org.sonatype.tycho</groupId>
           <artifactId>tycho-maven-plugin</artifactId>
           <version>${tycho-version}</version>
           <extensions>true</extensions>
	 </plugin>
	 <plugin>
	   <groupId>org.sonatype.tycho</groupId>
	   <artifactId>maven-osgi-source-plugin</artifactId>
	   <version>${tycho-version}</version>
	   <executions>
	     <execution>
	       <id>plugin-source</id>
	       <goals>
		 <goal>plugin-source</goal>
	       </goals>
	     </execution>
	   </executions>
	 </plugin>
       </plugins>

     </build>
     -->
</project>
